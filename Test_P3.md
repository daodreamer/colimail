---
  P3 优先级测试方案

  一、测试目标定位

  P3 的核心定位：提升测试深度和广度，从冒烟测试扩展到完整功能验证

  - P0: 核心逻辑单元测试 ✅ 已完成
  - P1: 集成测试 + 复杂业务逻辑 ✅ 已完成
  - P2: E2E 冒烟测试（启动 + UI 渲染） ✅ 已完成
  - P3: 完整功能流程测试 + 性能 + 安全性

  ---
  二、P3 测试方案（按优先级排序）

  方案 1: IMAP/SMTP 协议集成测试 ⭐⭐⭐⭐⭐

  测试范围：
  - 邮件收取完整流程（IMAP 连接 → 认证 → 文件夹列表 → 邮件头获取 → 邮件体下载）
  - 邮件发送完整流程（SMTP 连接 → 认证 → 邮件发送 → 附件处理）
  - IDLE 推送通知机制（新邮件事件、标记变更事件）
  - 错误场景处理（连接超时、认证失败、网络中断）

  技术方案：
  工具选择:
  - Rust: 使用 mock IMAP/SMTP 服务器库（greenmail-rust 或手写 mock）
  - 测试框架: cargo test + tokio::test

  实现方式:
  1. 启动本地 mock IMAP 服务器（监听 localhost:1143）
  2. 模拟真实邮件服务器响应（OK/NO/BAD/BYE）
  3. 注入测试邮件数据（包含各种编码格式）
  4. 调用实际的 IMAP/SMTP 命令代码
  5. 验证解析结果和错误处理

  预期测试用例（约 15-20 个）：
  - ✅ IMAP LOGIN 成功/失败
  - ✅ LIST 返回文件夹树结构
  - ✅ FETCH 解析 RFC 2047 编码的主题/发件人
  - ✅ FETCH 解析多部分 MIME 邮件（HTML + 附件）
  - ✅ IDLE 接收 EXISTS 事件并同步新邮件
  - ✅ SMTP STARTTLS 连接加密
  - ✅ SMTP 发送带附件的邮件（Content-Type: multipart/mixed）
  - ✅ 网络超时场景的重试逻辑
  - ✅ 认证失败后的错误提示

  理由：
  1. 当前缺失严重：IMAP/SMTP 是邮件客户端的核心功能，但完全没有自动化测试覆盖
  2. Bug 高发区：邮件编码解析、MIME 多部分处理、错误处理是历史上最容易出问题的地方
  3. 回归风险高：如果后续升级 imap 或 lettre 库版本，可能导致破坏性变更
  4. 用户影响大：邮件收发失败直接影响核心用户体验

  预估工作量：8-10 小时
  - Mock 服务器搭建：3 小时
  - 测试用例编写：5 小时
  - 调试和修复发现的 Bug：2 小时

  ---
  方案 2: E2E 功能流程测试 ⭐⭐⭐⭐

  测试范围：
  - 完整用户工作流：添加账户 → 加载文件夹 → 显示邮件列表 → 查看邮件详情 → 标记已读/星标 → 删除邮件
  - 多账户切换场景
  - 邮件搜索功能
  - 撰写和发送邮件（包含附件）

  技术方案：
  工具选择:
  - 继续使用 WebDriverIO + tauri-driver（已有基础设施）
  - 数据准备: 使用 SQLite 数据库 fixture（预填测试账户和邮件数据）

  实现方式:
  1. 创建测试数据库 fixture (test-data.db)
     - 预置 2 个测试账户（Gmail + Outlook）
     - 预置 50 封测试邮件（包含已读/未读/星标/附件等状态）
  2. 测试前: 复制 fixture 到应用数据目录
  3. 运行 E2E 测试: 模拟用户操作
  4. 测试后: 清理数据库

  预期测试用例（约 10-12 个）：
  - ✅ 账户列表显示预置的 2 个账户
  - ✅ 点击账户后加载 INBOX 文件夹邮件列表
  - ✅ 邮件列表显示正确的邮件数量（50 封）
  - ✅ 点击邮件后右侧显示邮件详情（主题、正文、附件）
  - ✅ 点击"标记为已读"按钮后邮件变为已读状态
  - ✅ 点击星标按钮后邮件列表显示星标图标
  - ✅ 搜索框输入关键词后邮件列表正确过滤
  - ✅ 切换到"未读邮件"过滤器后仅显示未读邮件
  - ✅ 删除邮件后邮件列表更新（邮件数 -1）
  - ✅ 切换到另一个账户后正确加载该账户的邮件
  - ✅ 撰写邮件界面打开并能输入内容
  - ✅ 附件上传功能正常工作

  理由：
  1. 补充 P2 缺失：P2 仅验证应用启动，未覆盖任何实际功能
  2. 防止严重回归：确保核心用户路径不会因重构而中断
  3. 基于真实数据：使用 fixture 模拟真实邮件场景（比 mock IMAP 更接近生产环境）
  4. 用户视角验证：从用户操作角度验证所有 UI 交互正确性

  预估工作量：6-8 小时
  - 数据库 fixture 准备：2 小时
  - E2E 测试用例编写：4 小时
  - 调试和修复 UI 问题：2 小时

  ---
  方案 3: 性能基准测试 (Performance Benchmarks) ⭐⭐⭐

  测试范围：
  - 邮件解析性能（1000 封邮件的主题/发件人解码时间 < 5 秒）
  - 数据库查询性能（INBOX 列表查询延迟 < 50ms）
  - UI 渲染性能（邮件列表滚动帧率 > 30 FPS）
  - 内存占用（空闲时 < 80MB，加载 1000 封邮件后 < 150MB）

  技术方案：
  后端性能测试 (Rust):
  - 使用 criterion.rs 进行 benchmark
  - 测试 codec.rs 的 decode_header 性能
  - 测试数据库批量插入/查询性能

  前端性能测试 (TypeScript):
  - 使用 Vitest 的 bench API 测试渲染性能
  - 使用 Chrome DevTools Performance API 测量帧率

  工具:
  - criterion = "0.5" (Rust benchmark)
  - vitest bench (前端 benchmark)

  预期基准指标：
  // 后端 benchmark 示例
  #[bench]
  fn bench_decode_1000_headers(b: &mut Bencher) {
      let headers = generate_test_headers(1000);
      b.iter(|| {
          for header in &headers {
              decode_header(header);
          }
      });
  }
  // 期望: < 5 秒 (平均每个 < 5ms)

  理由：
  1. 性能承诺：项目 development_requirements.md 中明确定义了性能目标（启动 ≤ 1.5s，内存 ≤ 80MB）
  2. 回归检测：防止未来代码变更导致性能下降
  3. 量化指标：建立可追踪的性能基线，便于 CI/CD 集成

  预估工作量：4-5 小时
  - Benchmark 基础设施搭建：2 小时
  - 关键路径性能测试编写：2 小时
  - 性能报告生成和优化建议：1 小时

  ---
  方案 4: CMVH 区块链集成测试 ⭐⭐

  测试范围：
  - CMVH 签名生成和验证流程
  - 链上验证 RPC 调用（使用 Infura/Alchemy 测试网）
  - 离线签名场景
  - WalletConnect 连接流程

  技术方案：
  工具选择:
  - 使用 Ethereum 测试网络（Sepolia）
  - Mock WalletConnect 会话
  - 使用已有的 cmvh 测试（12 个单元测试）扩展为集成测试

  实现方式:
  1. 创建测试钱包地址（私钥存储在环境变量）
  2. 部署测试版 CMVH 合约到 Sepolia
  3. 测试签名邮件 → 上链验证完整流程
  4. 测试离线签名 → 本地验证流程

  预期测试用例（约 5-7 个）：
  - ✅ 生成 EIP-712 签名并验证格式正确
  - ✅ 使用测试钱包签名邮件并提交到 Sepolia
  - ✅ 从链上读取签名并验证有效性
  - ✅ 离线验证签名（无需 RPC 调用）
  - ✅ WalletConnect 连接和断开流程
  - ✅ 签名失败场景（用户取消、网络错误）

  理由：
  1. 差异化功能：CMVH 是 Colimail 的核心创新点，需要高质量测试保障
  2. 区块链复杂性：涉及多方交互（前端、后端、钱包、链上合约），集成测试必不可少
  3. 用户信任：加密验证的准确性直接关系到用户信任

  预估工作量：5-6 小时
  - 测试网络环境搭建：2 小时
  - 集成测试用例编写：3 小时
  - 调试和优化：1 小时

  ---
  方案 5: 安全性测试 (Security Testing) ⭐⭐

  测试范围：
  - XSS 注入防护（邮件 HTML 内容渲染）
  - SQL 注入防护（数据库查询参数化）
  - 敏感数据加密（密码存储、邮件加密）
  - 深度链接安全（防止恶意链接劫持）

  技术方案：
  工具选择:
  - OWASP ZAP / Burp Suite（自动化扫描）
  - 手工测试（针对性攻击场景）
  - 代码审计（使用 cargo-audit 检查依赖漏洞）

  测试场景:
  1. XSS: 发送包含 <script>alert('XSS')</script> 的邮件
  2. SQL 注入: 账户名包含 ' OR '1'='1
  3. 路径遍历: 附件路径包含 ../../../etc/passwd
  4. CSRF: 深度链接 colimail://delete-all-emails

  预期测试用例（约 8-10 个）：
  - ✅ HTML 邮件中的 <script> 标签被正确过滤
  - ✅ 邮件正文中的外部链接点击弹出警告
  - ✅ 数据库查询使用参数化，防止 SQL 注入
  - ✅ 密码使用 keyring 安全存储，未明文记录在日志
  - ✅ 附件下载路径经过校验，防止路径遍历
  - ✅ 深度链接需要用户确认才能执行敏感操作
  - ✅ 依赖库无已知 CVE 漏洞（cargo audit 通过）
  - ✅ HTTPS 证书验证开启（防止中间人攻击）

  理由：
  1. 用户数据安全：邮件客户端处理高度敏感的用户数据（密码、邮件内容）
  2. 攻击面分析：邮件 HTML 渲染是最大的 XSS 攻击面
  3. 合规要求：桌面应用也需要遵循基本的安全最佳实践

  预估工作量：3-4 小时
  - 自动化扫描和报告分析：1 小时
  - 手工安全测试：2 小时
  - 漏洞修复验证：1 小时

  ---
  方案 6: 跨平台兼容性测试 ⭐⭐

  测试范围：
  - Windows (WebView2) ✅ 已完成
  - macOS (WKWebView)
  - Linux (webkit2gtk)

  技术方案：
  基础设施:
  - 使用 GitHub Actions 矩阵构建
  - 每个平台运行相同的 E2E 测试套件

  配置示例:
  strategy:
    matrix:
      os: [windows-latest, macos-latest, ubuntu-latest]
  steps:
    - run: npm run test:e2e

  预期验证点：
  - ✅ 应用在三个平台均能启动
  - ✅ 系统托盘在 Windows/macOS/Linux 均正常显示
  - ✅ 文件选择器（附件上传）在各平台正常工作
  - ✅ 通知权限在各平台正确请求

  理由：
  1. 跨平台承诺：Tauri 标榜跨平台，但需验证实际兼容性
  2. 用户覆盖：Linux 用户可能是技术早期采用者，需要支持
  3. CI/CD 集成：自动化跨平台测试防止特定平台的回归

  预估工作量：4-5 小时（假设已有 CI/CD 基础）
  - macOS 环境配置：2 小时
  - Linux 环境配置：2 小时
  - 调试平台特定问题：1 小时

  ---
  三、P3 测试优先级排序和实施建议

  推荐实施顺序

  | 优先级 | 测试方案             | 工作量   | ROI（投资回报） | 建议时间      |
  |-----|------------------|-------|-----------|-----------|
  | 1   | IMAP/SMTP 协议集成测试 | 8-10h | ⭐⭐⭐⭐⭐ 最高  | 立即实施      |
  | 2   | E2E 功能流程测试       | 6-8h  | ⭐⭐⭐⭐⭐ 最高  | 紧随其后      |
  | 3   | 性能基准测试           | 4-5h  | ⭐⭐⭐⭐ 高    | 第三周       |
  | 4   | 安全性测试            | 3-4h  | ⭐⭐⭐⭐ 高    | 发布前必做     |
  | 5   | CMVH 集成测试        | 5-6h  | ⭐⭐⭐ 中等    | 如有余力      |
  | 6   | 跨平台兼容性测试         | 4-5h  | ⭐⭐ 较低     | CI/CD 成熟后 |

  最小可行 P3 方案（MVP）

  如果时间有限，建议仅实施前 3 项：
  1. IMAP/SMTP 集成测试（8-10h）→ 保证核心功能正确性
  2. E2E 功能流程测试（6-8h）→ 保证用户路径完整性
  3. 性能基准测试（4-5h）→ 保证性能承诺达标

  总工作量：18-23 小时（约 3 个工作日）

  ---
  四、实施后的预期成果

  完成 P3 测试后，项目将达到：

  | 测试维度      | P0/P1/P2 现状      | P3 目标                   |
  |-----------|------------------|-------------------------|
  | 测试数量      | 179              | 230-250                 |
  | 覆盖率       | ~85% (关键路径)      | ~90% (全功能)              |
  | 测试类型      | 单元 + 集成 + E2E 冒烟 | + 协议集成 + 性能 + 安全        |
  | 平台覆盖      | Windows 仅        | Windows + macOS + Linux |
  | CI/CD 成熟度 | 本地运行             | 全自动化 + 跨平台矩阵            |

  ---
  五、总结

  核心建议：

  1. 必做项（P3 核心价值）：
    - ✅ IMAP/SMTP 协议集成测试
    - ✅ E2E 功能流程测试
  2. 强烈推荐（提升质量保障）：
    - ✅ 性能基准测试
    - ✅ 安全性测试
  3. 可选项（锦上添花）：
    - CMVH 集成测试（如果该功能是核心卖点则升级为必做）
    - 跨平台兼容性测试（如果用户群包含 Linux/macOS 用户）

  预计总投入：20-30 小时（约 1 周）
  预期收益：
  - 🛡️ 防止 90% 以上的严重回归
  - 📊 建立性能和安全基线
  - 🚀 支持快速迭代和重构
  - ✅ 达到企业级质量标准